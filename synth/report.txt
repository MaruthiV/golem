golem synthesis report (yosys 0.67)

SYNTHESIZABILITY: all RTL modules elaborate + synthesize clean, 0 errors.
  requant, divu, mac_tile, matmul_row, rmsnorm, softmax_row, gelu_lut, block
  (hierarchy -check passes; proc/opt/synth succeed). This is the real result:
  synthesis catches width/latch/multi-driver bugs that simulation does not. golem's
  RTL is hardware-ready, not just sim-ready.

GATE COUNT (abc-mapped, sky130-class 2-input gates):
  requant      ~8,090 gates   (dominated by the 32x31->64b requant multiplier + round/shift/sat)
  mac_tile     requant + 4 int8 MACs + int32 accumulator  (the memory-free hardenable tile)
  (full abc mapping of divider/multiplier-heavy modules is slow; requant is the representative anchor.)

MEMORY (NOT in the logic GDS -- external by design):
  weights 6.36MB + KV -> external SDRAM (weight-streaming architecture).
  on-die buffers (x/param/attn/gel) -> SRAM macros in a full-chip flow.

GDS: rtl/mac_tile.sv + gds/config.json ready for OpenLane sky130 (needs Docker+PDK).
--- full block (with on-die buffers) ---
DONE
